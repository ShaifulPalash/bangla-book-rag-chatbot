# Test Results

This file will hold the actual output of running the 10 required test
questions against the real, deployed chatbot — generated by running:

```bash
python tests/test_retrieval.py
```

> **Status: not yet run.** This placeholder will be replaced with real
> results (question, actual chatbot answer, actual citation, pass/fail)
> once the full pipeline (crawler → chunker → vector index → API key) is
> set up and the command above is run for real.

## How to fill this in

1. Complete all setup steps in the main [README](../README.md#-setup--installation).
2. Run `python tests/test_retrieval.py` and copy its table output below.
3. For each question, also paste the full answer text (not just the
   table summary) so a grader can see exactly what the chatbot said.
4. Replace this placeholder section with the real results table.

---

## Results

_(paste real output here)_
